import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { JhiDataUtils } from 'ng-jhipster';

import { ITourgroup } from 'app/shared/model/tourgroup.model';

@Component({
  selector: 'jhi-tourgroup-detail',
  templateUrl: './tourgroup-detail.component.html'
})
export class TourgroupDetailComponent implements OnInit {
  tourgroup: ITourgroup | null = null;

  constructor(protected dataUtils: JhiDataUtils, protected activatedRoute: ActivatedRoute) {}

  ngOnInit(): void {
    this.activatedRoute.data.subscribe(({ tourgroup }) => (this.tourgroup = tourgroup));
  }

  byteSize(base64String: string): string {
    return this.dataUtils.byteSize(base64String);
  }

  openFile(contentType: string, base64String: string): void {
    this.dataUtils.openFile(contentType, base64String);
  }

  previousState(): void {
    window.history.back();
  }
}
